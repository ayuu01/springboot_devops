
pipeline {
    agent any
    

    stages {
        stage('Build project') {
            steps {
               bat 'mvn clean -f SpringBootDevops/pom.xml'
            }
        }
       stage('Package') {
            steps {
               bat 'mvn package -f SpringBootDevops/pom.xml'
            }
        }
    }
}
